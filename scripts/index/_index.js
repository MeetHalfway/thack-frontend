/*
* This module contain global configuration.
*/
(function() {

  'use strict';

  angular
    .module('app.index', [
      'app.core'
    ])
    .run(run);

  function run($rootScope) {
    // do whatever
  }
  
})();
